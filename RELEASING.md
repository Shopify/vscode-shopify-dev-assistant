# Releasing Shopify Dev Assistant

- Bump the version in `package.json`
- Make sure that tests are passing in CI
- Create a new release on GitHub
- The release will trigger a deployment to the VS Code marketplace
- Done!
